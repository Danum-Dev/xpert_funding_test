import 'package:flutter/material.dart';

/// ===========================================================
/// App Gradient Colors
/// ===========================================================

/// Gradient colors for general page backgrounds.
const List<Color> pageBgGradientColors = [
  Color(0xFF000F33),
  Color(0xFF070707),
];

/// Gradient colors for card backgrounds.
const List<Color> cardGradientColors = [
  Color(0xFF133078),
  Color(0xFF0F182D),
];

/// ===========================================================
/// Card & Badge Colors
/// ===========================================================

/// Border color for card widgets.
const Color cardBorderColor = Color(0xFF355191);

/// Drop shadow color for Pro badge text.
const Color proBadgeFontDropShadowColor = Color(0xFFABC2FF);

/// Main background color for Pro badge.
const Color proBadgeColor = Color(0xFF0249FE);

/// Gradient colors for badges (e.g., Pro badge).
const List<Color> badgeGradientColors = [
  Color(0xFF0249FE),
  Color(0xFF80A4FE),
];

/// Main text color for Challenge badge.
const Color challengeBadgeColor = Color(0xFF80A4FE);

/// Background color for Challenge badge.
const Color challengeBgBadgeColor = Color(0xFF11285F);

/// Drop shadow color for Challenge badge text.
const Color challengeBadgeFontDropShadowColor = Color(0xFFABC2FF);

/// Main text color for Funded badge.
const Color fundedBadgeColor = Color(0xFF75DFA7);

/// Background color for Funded badge.
const Color fundedBgBadgeColor = Color(0xFF053321);

/// Drop shadow color for Funded badge text.
const Color fundedBadgeFontDropShadowColor = Color(0xFFABC2FF);

/// Active step progress color.
const Color stepProgressActiveColor = Color(0xFF3A6BFF);

/// Background color for active step progress.
const Color stepProgressActiveBgColor = Color(0xFF5865F2);

/// ===========================================================
/// Dashboard Button Colors
/// ===========================================================

/// Text color for dashboard buttons.
const Color dashboardButtonLabelColor = Color(0xFF070707);

/// ===========================================================
/// Fonts
/// ===========================================================

/// Global font family used throughout the application.
const String fontFamily = "InterTight";

/// ===========================================================
/// Asset Paths (SVG Icons)
/// ===========================================================

/// Path for bar chart icon asset.
const String barChartIcon = "assets/icons/bar-chart.svg";

/// Path for dashboard icon asset.
const String dashboardIcon = "assets/icons/dashboard.svg";

/// Path for lock icon asset.
const String lockIcon = "assets/icons/lock-icon.svg";
